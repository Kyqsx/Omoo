# Copie para ".env" localmente. Na Vercel, configure em
# Project Settings > Environment Variables (mesmo nome).
#
# URL pública do backend na Railway (sem barra no final).
# Localmente, aponte para o servidor rodando na sua máquina.
VITE_BACKEND_URL=http://localhost:3000
